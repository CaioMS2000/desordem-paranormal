# The system is integrated with:
    - Automatic daily builds via cron (3:00 AM)
    - Manual build trigger via REST API
    - Manual cleanup trigger via REST API
    - Event-driven cleanup after build completion